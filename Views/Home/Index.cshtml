@model IEnumerable<Post>
@{
    ViewBag.Title = "Blog Website";
    ViewBag.Description = "This blog is here.";
    ViewBag.Keywords = "Blog Programming Travel Photography";
    var base_path = Context.Request.PathBase;
}


@*<h2>Home Page</h2>
    <img src="~/content/static/profile 1.png" width="500" />*@

<div class="main-img">
    <span class="title">Placeholder Name</span>
</div>

<div class="container">
    @*<ul>
            @foreach (var post in Model)
            {
                <li>
                    <a asp-controller="Home" asp-action="Post" asp-route-id="@post.Id">
                        @post.Title
                    </a>
                </li>
            }
        </ul>*@

    @foreach (var post in Model)
    {
        var image_path = $"{base_path}/Image/{post.Image}";
        <a class="post" asp-controller="Home" asp-action="Post" asp-route-id="@post.Id">
            
            <img src="@image_path"/>
            <span class="title">
                @post.Title
            </span>
        </a>
    }
</div>